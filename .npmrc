# Optimize npm install for CI/production builds
production=true
legacy-peer-deps=false
progress=false

# Cache optimization
cache-min=999999

# Network settings
fetch-timeout=120000
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-retries=5

# Disable optional dependencies to speed up install
optional=false
